Before release:
  Commands:
    manual dr
    manual receive
    manual send
    mark delivered
    mark followup
  CLI issues:
    Handling of invocation in non-incident directory
    Collection of required configuration settings
    Auto-update forms when using CLI

When possible:
  PDF support for Unicode
  Update wppsvr, packet-ex
  Incident archive
  Field comparisons
